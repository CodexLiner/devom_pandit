package com.devom.pandit.app.models

enum class RepeatOption(val label: String) {
    WEEKLY("Save this slot for the same time upcoming week"),
    MONTHLY("Save this slot for the same time upcoming month")
}
